import 'package:flutter/material.dart';

abstract class AppColor {
  static Color primary = Colors.black.withValues(alpha: 0.7);
  static Color white = Colors.white;
  static Color red = Colors.red;
}
